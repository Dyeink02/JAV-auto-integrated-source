# 版本更新记录 - 0.23.0（收尾清理）
- 日期：`2026-04-25`
- 版本：`0.23.0`
- 类型：稳定性收尾（不变更版本号）

## 更新内容
1. 清理 `organizerController` 与 `adLearningService` 中重复函数定义导致的覆盖风险。
2. 保留 AI 模型可视化配置链路，确保前后端参数一致。
3. 重新执行编码检查、语法检查与 TypeScript 构建。
4. 重新打包便携版 EXE，输出可直接测试的交付包。

## 验证结果
- 编码检查：通过
- JS 语法检查：通过
- TypeScript 构建：通过
- 便携版打包：通过
