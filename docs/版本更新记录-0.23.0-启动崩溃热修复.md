# 版本更新记录 - 0.23.0 启动崩溃热修复
- 日期：`2026-04-25`
- 类型：热修复（不改版本号）

## 修复项
1. 修复 `adLearningService` 中 `DEFAULT_AD_MODEL_TYPE` 的初始化顺序问题。
2. 消除主进程启动时报错：`Cannot access 'DEFAULT_AD_MODEL_TYPE' before initialization`。
3. 重新验证便携包与免安装目录版启动稳定性。

## 影响范围
- `desktop/mainServices/adLearningService.js`
- 打包产物启动流程（主进程加载阶段）
